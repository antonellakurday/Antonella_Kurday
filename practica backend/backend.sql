create table persona (
dni INT(20) primary key,
nombre VARCHAR(30) NOT NULL,
apellido VARCHAR(30) NOT NULL
);

create table usuario (
mail varchar (40) primary key,
nikname varchar(20) not null,
pasword varchar(20) not null
);
